name: mypy
on: [push, pull_request]
jobs:
  mypy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v2
      - run: pip install mypy
      - run: mypy --install-types --non-interactive fancy_dataclass tests
